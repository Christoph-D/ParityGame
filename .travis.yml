language: isabelle

before_install:
  - "wget http://isabelle.in.tum.de/website-Isabelle2016-RC5/dist/Isabelle2016-RC5_app.tar.gz"
  - "tar -xzf Isabelle2016-RC5_app.tar.gz"
  - "mkdir -p ~/.isabelle/Isabelle2016-RC5/heaps/polyml-5.6_x86-linux/"
  - "pushd ~/.isabelle/Isabelle2016-RC5/heaps/polyml-5.6_x86-linux/"
  - "wget http://yozora.eu/Isabelle2016-RC5_heap_prebuilt.tar.gz"
  - "tar -xzf Isabelle2016-RC5_heap_prebuilt.tar.gz"
  - "popd"

addons:
  apt:
    packages:
      - lib32stdc++6
      - texlive-latex-base
      - texlive-latex-recommended
      - texlive-fonts-recommended

script:
  - "cd ParityGame && ../Isabelle2016-RC5/bin/isabelle build -o document=pdf -j 2 -v -d ../Coinductive/ -D ."
